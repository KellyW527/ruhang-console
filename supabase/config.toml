project_id = "xhkthstyxqbqymwvrdgb"

# create-checkout 需要登录态,验证 JWT
[functions.create-checkout]
verify_jwt = true

# stripe-webhook 来自 Stripe,不能验证 JWT,改用 webhook 签名校验
[functions.stripe-webhook]
verify_jwt = false

# redeem-quota 用户从配额选项目,需要登录态
[functions.redeem-quota]
verify_jwt = true

# boss-reply: NPC 回复，调 MiMo，需要登录态
[functions.boss-reply]
verify_jwt = true

# ai-summary: 项目结业能力画像，调 MiMo，需要登录态
[functions.ai-summary]
verify_jwt = true

# evaluate-submission: 读取用户交付件并生成 AI 任务评价，调 MiMo，需要登录态
[functions.evaluate-submission]
verify_jwt = true
